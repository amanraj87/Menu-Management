/**
 * Legacy REST API client. The app now uses GraphQL via @/shared/graphql/hooks.
 * Kept for reference or if you add a REST bridge in Services.
 */
export const api = {} as Record<string, unknown>
